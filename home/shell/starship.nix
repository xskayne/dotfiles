{
  programs.starship = {
    # enable the starship prompt (a fast and customizable prompt for the shell)
    enable = true;

    settings = {
      # additional settings for the starship prompt (currently empty)
    };
  };
}
